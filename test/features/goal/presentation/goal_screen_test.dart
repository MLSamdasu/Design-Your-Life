// TODO: REST API 마이그레이션 후 테스트 재작성 필요
// StreamProvider → FutureProvider 전환, MockApiClient 사용으로 전환
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('REST API 마이그레이션 후 재작성 예정', () {
    // Provider/화면 테스트는 FutureProvider + MockApiClient 기반으로 재작성한다
    expect(true, true);
  });
}
